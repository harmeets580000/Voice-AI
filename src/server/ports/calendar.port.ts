/**
 * Calendar PORT (Phase 1 optional / Phase 6) — stub. A Google Calendar adapter will
 * implement one-way write now (optional) and two-way sync later. No methods used yet.
 */
export interface CalendarProvider {
  readonly name: string;
}
